import React from 'react';

import { styled } from 'shared/theme';

export const PhoneWrapper = styled.div.attrs({})<React.HTMLAttributes<HTMLDivElement>>`
  background: linear-gradient(180deg, rgba(255, 255, 255, 0) 0%, #ffffff 82.29%);
`;
