import TravelChecklist from 'modules/TravelChecklist';
import { NextPage } from 'next';

const TravelChecklistPage: NextPage = () => {
  return <TravelChecklist />;
};

export default TravelChecklistPage;
